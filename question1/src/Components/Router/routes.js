/* eslint-disable import/no-cycle */
import HomePage from '../Pages/HomePage';
import PhotosPage from '../Pages/NewPage';

const routes = {
  '/': HomePage,
  '/photos': PhotosPage,
};

export default routes;
